This project demonstrates how to containerize a simple Flask web application using Docker.
The Dockerfile builds the image with Python dependencies, while Docker Compose is used to build and run the container with port mapping.
The application runs inside the container on port 3000 and is exposed to the host machine on port 3001.